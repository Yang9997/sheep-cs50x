1. 注意变量单位范围，可能会溢出然后循环截断。在helpers.c的Sepia函数和Blur函数里面都有标注
2. 整体修正后再进行值修改，尤其是数组可能会有这种要求，这种情况下不要改一个存一个，修改原值会影响后面要修改的变量的值。在helpers.c的Blur函数里面有标注
3. math库里面有round函数用于double的四舍五入
4. reflect函数处注意用于对称时的中间位置单双数大于号小于号，原函数中有标注
Sepia函数那里，要是用BYTE变量的话会变成变化后图片中建筑物正常，天空太阳之类的环境会像热力图那个颜色类似的情况，已经修正了。

