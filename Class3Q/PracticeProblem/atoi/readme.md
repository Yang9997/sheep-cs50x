# 关于c语言的string和strlen


1. C 语言的字符串是 char[] 数组类型吗？

是的。在 C 语言中，字符串本质上就是一个字符数组。CS50 库中的 string 类型实际上就是 char *（指向字符数组第一个元素的指针）的别名。

2. strlen 是字符个数？

是的。strlen 计算的是可见字符的数量，不包括结尾的结束符。

3. char[] 总长度是字符个数 + 1？

是的。为了在内存中存储一个字符串，你必须多留一个位置给“结束符”（Null Terminator），即 \0。
